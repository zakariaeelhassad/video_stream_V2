server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: file://${user.home}/Desktop/Streaming_Video/config-repo
          clone-on-start: true
          default-label: master
